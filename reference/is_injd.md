# Check if an object is of class `injd`

Check if an object `x` is of class `injd`.

## Usage

``` r
is_injd(x)
```

## Arguments

- x:

  any R object.

## Value

A logical value: `TRUE` if `x` inherits from `injd` class, `FALSE`
otherwise.
